# Claude Code Instructions

Read and follow `AGENTS.md` before making any changes.

Do not upload data, expose credentials, modify raw legacy files, push directly
to `main`, or claim that results were reproduced without running the documented
evaluation.

Before finishing, run `make check` and summarize all changes and uncertainties.
