# Claude Code Instructions

Read and follow `AGENTS.md` before making any changes.

Do not add dataset preparation code, expose credentials, invent training
hyperparameters, push directly to `main`, or claim that results were reproduced
without running the documented evaluation.

Before finishing, run `make check` and summarise all changes and uncertainties.
