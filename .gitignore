# macOS
.DS_Store

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Secrets
.env
*.pem
*.key

# Hugging Face cache and any local dataset copies. The benchmark streams the
# dataset from the Hub, so nothing here is required to run it.
data/
.cache/

# Benchmark output. Results are committed deliberately, one at a time, not by
# a bulk `git add` -- a results file records the commit that produced it, so
# sweeping them in alongside code changes makes that record a lie.
results/*.npz

# Experiments and models
outputs/
checkpoints/
wandb/
*.pt
*.pth
*.ckpt
*.safetensors
*.bin

# IDE
.vscode/
.idea/
reports/local/
